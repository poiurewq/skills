# Q's Agent Skills (tested on Claude)

`/spec` - human-driven spec dev workflow. a good spec prevents many an implementation trouble. 

`/save-conversation` - per the name. simply saves your conversation for future reference.

`/managing-knowledge-base` - a suite of skills to extract and organize generalizable knowledge from your conversation or from links into a knowledge base.

