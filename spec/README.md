# spec

This skill has moved to its own repository: <https://github.com/poiurewq/spec>.

The history prior to the move lives in this repo's git log; the new repo was extracted with `git filter-repo --subdirectory-filter spec` so commits that touched `spec/` are preserved there as well.
